{
    "name": "aws-toolkit-lambda-esm-handler-repro",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "tsc",
        "package": "npm run build && cp package.json dist/package.json && cd dist && zip -r ../lambda.zip .",
        "clean": "rm -rf dist lambda.zip",
        "deploy": "npm run package && aws lambda update-function-code --function-name \"$FUNCTION_NAME\" --zip-file fileb://lambda.zip && aws lambda update-function-configuration --function-name \"$FUNCTION_NAME\" --handler index.handler --runtime nodejs24.x"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.162",
        "@types/node": "^24.0.0",
        "typescript": "^5.9.0"
    }
}
